
<div class="container">
  <%= simple_form_for([@article, @article.comments.build]) do |f| %>
    <p>
      <%= f.input :commenter %><br>
     
    </p>
    <p>
      <%= f.input :body %><br>
     
    </p>
    <p>
      <%= f.button:submit %>
    </p>
  <% end %>
</div>